# 📐 Spec-Driven Rules

## Spec must define:

- Input / Output schema
- Error behavior
- Flow decision
- Observability fields

## Spec must be:

- **Validatable** — fail fast at startup
- **Executable** — drive runtime behavior

## Spec drives:

- `code`
- `test`
- `runbook`
- `metrics`
