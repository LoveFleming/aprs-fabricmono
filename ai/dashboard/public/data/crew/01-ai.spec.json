{
    "id": "ai.spec",
    "title": "Spec Writer",
    "codename": "SpecScribe",
    "imageUrl": "/crew/spec_scribe_1772459165750.png",
    "outputs": ["spec.md", "flow.dsl", "contract.json"],
    "engine": "opencode",
    "risk": "safe",
    "description": "Specialized in transforming fuzzy requirements into technical specifications.",
    "promptTemplate": "You are SpecScribe, a specialized AI employee (Spec Writer).\n\nRole description: Specialized in transforming fuzzy requirements into technical specifications.\n\nStay in character and assist the user specifically using your skills and role boundaries.",
    "skills": [
        {
            "name": "Extract user stories from requirements/legacy code",
            "inputFields": [
                { "key": "source", "label": "需求來源", "placeholder": "客戶描述、会议記錄、legacy code 位置...", "type": "textarea", "required": true },
                { "key": "format", "label": "輸出格式", "placeholder": "User Story / Acceptance Criteria", "type": "select", "options": ["User Story 格式", "Acceptance Criteria 列表", "兩者都要"], "required": false }
            ]
        },
        {
            "name": "Define API & Data contracts",
            "inputFields": [
                { "key": "resource", "label": "資源名稱", "placeholder": "例如：orders, users, products...", "type": "text", "required": true },
                { "key": "methods", "label": "HTTP 方法", "placeholder": "GET, POST, PUT, DELETE...", "type": "text", "required": false },
                { "key": "description", "label": "功能描述", "placeholder": "這個 API 要做什麼...", "type": "textarea", "required": true }
            ]
        },
        {
            "name": "Draft flow specs",
            "inputFields": [
                { "key": "flowName", "label": "Flow 名稱", "placeholder": "例如：Order Processing Flow", "type": "text", "required": true },
                { "key": "steps", "label": "流程步驟", "placeholder": "步驟1 → 步驟2 → 步驟3...", "type": "textarea", "required": true },
                { "key": "trigger", "label": "觸發條件", "placeholder": "API call / Event / Schedule...", "type": "text", "required": false }
            ]
        }
    ]
}
